# 2026-08-01

Stashed the pre-existing uncommitted work instead of committing it. Recover it with
`git stash apply` against the entry named:

    freshen 672bb50f: pre-existing WIP

Stashed files and a best guess at the next step for each:

- doing.txt — staged but never committed running notes from the 2026-07-26 and 2026-07-27
  freshen passes. Two items are marked resolved and can be dropped; the two still open are
  the ruff `too-many-statements-in-try-clause` in `_run_shell_command` (fix by narrowing the
  try block to one failure mode rather than adding an inline ignore) and the POSTPROCESSORS
  "document" versus "root" key mismatch that makes the plugin inert end to end. Next step is
  to decide whether doing.txt belongs in git at all, then either commit the trimmed file or
  move the two open items into an issue and drop it.

Follow-ups from this pass:

- POSTPROCESSORS is still keyed on "document" while mdformat renders from a node of type
  "root", so the plugin remains inert end to end. Re-confirmed: mdformat.text with
  post_command = "tr a-z A-Z" returns its input unchanged. Left alone because it is a
  behavior change that deserves its own reviewed commit
- The repo's mise layout does not self-activate. .config/mise.toml sets MISE_ENV = "dev,ci"
  in [env], but mise resolves tools before that value takes effect, so python 3.10.15 and
  the pinned tox never land on PATH in a plain shell. Prefixing MISE_ENV=dev makes
  tox -e test-min and -e hook-min run locally, which the last two passes recorded as
  impossible. Either move the tools back into a root mise.toml (mdformat-gfm-alerts does
  this) or document the prefix
- _INJECTION_CASES in the security guard is still empty, which stays fine while the plugin
  registers no render sinks
