# Open work (last reviewed 2026-08-01)

1. scripts/canary_repos.json stays empty. An entry can now carry a post_command through its
   "options" field (example in AGENTS.md), but no public repo configures one and
   tox -e canary installs no hook binary. Worth revisiting if mdsf lands in the canary env.

2. _INJECTION_CASES in tests/render/test_security.py is empty. Fine while the plugin
   registers no render sinks; revisit if it ever gains one.

Watch: the pinned formatter versions in .config/mise.{ci,dev}.toml and the hougesen/mdsf
pin in tests.yml now hold tests/pre-commit-test.md stable, so nothing bumps them on its own.
Check for upstream releases when touching the fixture (see AGENTS.md for the bump order).
